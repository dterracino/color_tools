# Development dependencies
# Includes all optional features and development tools
# Install with: pip install -r requirements-dev.txt

# Base requirements
-r requirements.txt

# Optional fuzzy matching for validation module
-r requirements-fuzzy.txt

# Optional image processing features
-r requirements-image.txt

# Optional MCP server features
-r requirements-mcp.txt

# Code coverage
coverage>=7.0.0

# Type checking and linting (from pyproject.toml dev dependencies)
pyright>=1.1.0

# Building and publishing
build>=1.0.0
twine>=4.0.0
