# MCP server requirements
# Install with: pip install -r requirements-mcp.txt
# Or via extras: pip install color-match-tools[mcp]

# Base requirements
-r requirements.txt

# MCP SDK for Model Context Protocol server
mcp>=1.0.0
